
export enum ErrorCode {
    // 通用错误
    UNKNOWN = 1000,
    VALIDATION_ERROR = 1001,
    MISSING_PARAMETERS = 1002,
    INVALID_PARAMETERS = 1003,
    INVALID_INPUT = 1004,
    INVALID_UUID = 1005,
    INVALID_HASHED_PASSWORD = 1006,

    // 认证相关
    UNAUTHORIZED = 2000, // 未登录
    FORBIDDEN = 2001,     // 权限不足
    TOKEN_EXPIRED = 2002,
    IP_EXCEED_LIMIT = 2003, //IP restriction
    Turnstile_TOKEN_MISSING = 2004,

    // 用户相关
    USER_NOT_FOUND = 3000,
    USER_ALREADY_EXISTS = 3001,
    INVALID_CREDENTIALS = 3002,
    INVALID_TOKEN = 3003,
    USER_ROLE_INVALID = 3004,
    INVALID_PASSWORD = 3005,

    // 商品/订单相关（可根据业务扩展）
    PRODUCT_NOT_FOUND = 4000,
    PRODUCT_OUT_OF_STOCK = 4001,
    CART_EMPTY = 4002,
    PRODUCT_CATEGORY_TOPID_NOT_FOUND = 4003,

    ORDER_NOT_FOUND = 5000,
    ORDER_ALREADY_PAID = 5001,
    ORDER_CANCEL_FAILED = 5002,

    // 系统相关
    INTERNAL_SERVER_ERROR = 9000,
    SERVICE_UNAVAILABLE = 9001,
    DATABASE_ERROR = 9002,
    // CMS
    PAGEINSTANCE_NOT_FOUND = 9003,
    SECTIONINSTANCE_ID_NOT_FOUND = 9004,
    SECTIONINSTANCE_NOT_FOUND = 9005,

    // tenant相关
    TENANT_NOT_FOUND = 10000,

    // theme相关
    THEME_NOT_FOUND = 11000


}
